import { boot } from 'quasar/wrappers'
import { useAuthStore } from 'src/stores/auth'

export default boot(({ app, store }) => {
  // 不需要保留这个文件，相关内容已经在 /directives/permissions.js 中实现。
  // nothing left, there is no need of this, it's already in /directives/permissions.js
})
